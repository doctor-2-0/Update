import React from 'react';

// Define the component as a functional component
const HelloPatient: React.FC = () => {
    return (
        <div>
            <h1>Hello, Patient!</h1>
        </div>
    );
};

export default HelloPatient;
